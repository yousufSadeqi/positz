# Railway uses official image only if you rebuild without our fork patches.
# Preferred: railway.toml Nixpacks builds THIS fork (DISABLE_TEMPORAL + SKIP_AUTH).
# Keep this file for optional “stock Postiz image” deploys later.
FROM ghcr.io/gitroomhq/postiz-app:latest
EXPOSE 5000
