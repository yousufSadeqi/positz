# Railway deploys the official Postiz image (frontend + backend + orchestrator).
# External services: Neon (Postgres), Upstash (Redis), Temporal Cloud.
FROM ghcr.io/gitroomhq/postiz-app:latest

EXPOSE 5000
